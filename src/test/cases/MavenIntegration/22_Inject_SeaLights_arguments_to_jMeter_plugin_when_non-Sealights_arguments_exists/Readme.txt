In this test a pom exists with jMeter plugin and with 'arguments' element exists and
arguments that are not related to Sealights are already inside.
The test should verify that:
1. We append the sealights arguments inside the 'arguments' element.